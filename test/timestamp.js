const assert = require('assert').strict;

describe('Timestamp api', function() {
  
  describe('GET /api/timestamp/:valid_date_string', function() {
    it('should return JSON with unix and utc fields');
  });


});